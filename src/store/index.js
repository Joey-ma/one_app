import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  // this.$store.state.***
  state: {
  },
  // this.$store.commit('方法的名称', '按需传递唯一的参数')
  mutations: {
  },
  // store 的计算属性  this.$store.getters.***
  getter: {
  },
  // Action 提交的是 mutation，而不是直接变更状态
  actions: {
  },
  modules: {
  }
})
